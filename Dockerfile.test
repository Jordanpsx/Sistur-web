# Imagem só para rodar a suíte. Separada do Dockerfile de produção, que é
# multi-stage e descarta as devDependencies.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
CMD ["npm", "test"]
